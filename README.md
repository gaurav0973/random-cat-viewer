# Cat Viewer

A simple React app that shows a random cat image from an API.

## Features

- Fetches a random cat image
- Updates the image with a new request
- Simple and minimal UI

## Run Locally

```bash
npm install
npm run dev
```

## API Used

https://api.freeapi.app/api/v1/public/cats/cat/random